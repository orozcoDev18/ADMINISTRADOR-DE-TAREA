/** @type {import('next').NextConfig} */
const nextConfig = {
  // En Next.js 15/16 la configuración de ESLint se maneja fuera de este archivo
  // o con nuevas propiedades. Dejamos esto limpio para evitar errores.
};

export default nextConfig;
